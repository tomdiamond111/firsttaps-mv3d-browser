import 'package:flutter/material.dart';

class WhiteDividerWidget extends StatelessWidget {
  const WhiteDividerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider(color: Colors.white);
  }
}
